---
title: "Brand API"
description: "Example section for showcasing API endpoints"
---

<Warning>
  Brand API endpoints are available only for paid plans. If you are on a free
  plan, you can [upgrade here](https://logokit.com/pricing).
</Warning>

## Authentication

Brand API endpoints are authenticated using Bearer token authentication. You can find yours in the
[your account](https://logokit.com/account/api-tokens).

```bash
Authorization: Bearer: {{secret-api-token}}
```

## Get Brand Information

TODO

### Request

```bash
curl --header "Authorization: Bearer: {{secret-api-token}}" "https://api.logokit.com/brands/apple.com"
```

### Response

```json
{
  "id": "apple.com",
  "name": "Apple",
  "domain": "apple.com",
  "logo": {
    "url": "https://img.logokit.com/apple.com?token={your-publishable-token}",
    "width": 200,
    "height": 200
  },
  "colors": [
    {
      "color": "#000000"
    }
  ],
  "fonts": [
    {
      "fontFamily": "Helvetica Neue"
    }
  ]
}
```
